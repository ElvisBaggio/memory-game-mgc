services:
  memory-game:
    build: .
    container_name: memory-game-mgc
    restart: unless-stopped
    ports:
      - "80:3000"
    volumes:
      - game-data:/app/data
    environment:
      - DATA_DIR=/app/data

volumes:
  game-data:
