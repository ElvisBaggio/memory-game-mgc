services:
  memory-game:
    build:
      context: .
      args:
        VERSION: ${VERSION:-dev}
    container_name: memory-game-mgc
    restart: unless-stopped
    ports:
      - "80:3000"
    volumes:
      - game-data:/app/data
    environment:
      - DATA_DIR=/app/data

volumes:
  game-data:
